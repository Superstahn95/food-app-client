function AuthLoader() {
  return (
    <div className="absolute top-0 left-0 h-[100vh] w-[100vw] bg-black/60 flex items-center justify-center">
      Authenticating
    </div>
  );
}

export default AuthLoader;
